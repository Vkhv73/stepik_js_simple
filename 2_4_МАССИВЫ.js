/*Как осуществляется запись в массив мы уже видели в предыдущем шаге:*/

    var myArray = new Array();
        myArray[0] = "Иванов";   // запись 0-го элемента в массив myArray
        myArray[1] = "Jonson";   // запись 1-го элемента в массив myArray
        //Чтение осуществляется аналогично :

    var x = myArray[1]; // в результате в переменной x окажется значение первой ячейки массива - строковое значение "Иванов".


console.log(x);